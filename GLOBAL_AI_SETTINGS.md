# 全局AI设置使用指南

## 🎯 功能概述

现在系统支持全局AI模型配置，一次选择，全局生效！所有AI功能都将使用你选择的模型：

- ✅ 论文摘要生成
- ✅ 创新点提取
- ✅ 代码架构分析
- ✅ PDF翻译（未来）
- ✅ AI对话（未来）

## 📍 入口位置

在**任意页面**的导航栏右侧，点击：

```
⚙️ AI设置 [Gemini]
```

## 🖥️ 使用步骤

### 1. 打开AI设置

点击导航栏的"⚙️ AI设置"，会弹出设置面板。

### 2. 查看当前模型

面板顶部显示当前使用的AI模型：

```
🎯 当前使用的模型
┌──────────────────────────────┐
│ Google Gemini                 │
│ gemini-2.0-flash-exp          │
│ 性能强，完全免费 | 免费        │
└──────────────────────────────┘
```

### 3. 选择AI模型

下方显示所有支持的模型：

**已配置的模型**（可选择）：
```
Google Gemini (gemini-2.0-flash-exp)    ✓ 当前使用
性能强，完全免费 | 成本: 免费             [使用中]

Anthropic Claude (claude-3-5-haiku-...)
推理能力最强 | 成本: 付费                  [选择]

DeepSeek (deepseek-chat)
性价比极高 | 成本: 极低成本               [选择]
```

**未配置的模型**（灰色禁用）：
```
OpenAI GPT (gpt-4o-mini) ⚠️ 未配置
业界标杆 | 成本: 付费                     [需要配置]
```

### 4. 切换模型

点击"选择"按钮即可切换到对应的模型。系统会：
- ✓ 立即切换到新模型
- ✓ 保存选择到浏览器（跨页面、跨会话）
- ✓ 显示成功提示

### 5. 配置新模型

点击"查看配置指南"展开详细说明，包含：
- .env文件配置方法
- Vercel环境变量配置
- 推荐模型组合

## 🌐 全局生效范围

模型选择会在**所有页面、所有AI功能**中生效：

| 功能 | 页面 | 生效范围 |
|------|------|----------|
| 论文摘要 | 论文详情 | ✓ 使用全局模型 |
| 创新点提取 | 论文详情 | ✓ 使用全局模型 |
| 代码架构分析 | 论文详情 | ✓ 使用全局模型 |
| 未来的对话功能 | 所有页面 | ✓ 使用全局模型 |

## 💾 持久化存储

选择的模型会保存在浏览器的 `localStorage` 中：

- ✓ 刷新页面后依然有效
- ✓ 关闭浏览器后依然有效
- ✓ 跨页面共享（论文列表、详情页、关系图）
- ⚠️ 不同浏览器需要分别配置

## 🎨 导航栏标识

导航栏的"AI设置"按钮会显示当前模型的简称：

```
⚙️ AI设置 [Gemini]     ← 使用Gemini
⚙️ AI设置 [Claude]     ← 使用Claude
⚙️ AI设置 [未配置]     ← 没有配置任何模型
```

颜色标识：
- 🟢 绿色：已配置并选择
- 🟡 黄色：未配置任何模型

## ⚡ 快速配置指南

### 本地开发

编辑 `.env` 文件：

```bash
# 推荐：Gemini（完全免费，性能强）
GEMINI_API_KEY=your_gemini_api_key
GEMINI_MODEL=gemini-2.0-flash-exp

# 或者：Claude（推理能力强，公司可用）
CLAUDE_API_KEY=sk-ant-api03-xxx
CLAUDE_MODEL=claude-3-5-haiku-20241022
```

重启应用：
```bash
python -m uvicorn backend.main:app --reload
```

### Vercel部署

1. 打开 Vercel 项目
2. Settings → Environment Variables
3. 添加：
   - Name: `GEMINI_API_KEY`
   - Value: `your_key_here`
4. 点击"Save"
5. 重新部署（Vercel会自动触发）

## 📊 支持的AI模型

| 模型 | ID | 成本 | 特点 | 推荐场景 |
|------|----|----|------|---------|
| Google Gemini | gemini | 免费 | 性能强，完全免费 | 日常使用 |
| 智谱AI GLM-4 | zhipu | 免费 | 中文强，国内访问 | 国内用户 |
| DeepSeek | deepseek | 极低 | 性价比最高 | 成本敏感 |
| 通义千问 | qwen | 廉价 | 稳定快速 | 阿里云用户 |
| Anthropic Claude | claude | 付费 | 推理最强 | 复杂分析 |
| OpenAI GPT | openai | 付费 | 业界标杆 | 企业应用 |
| Groq | groq | 免费 | 速度极快 | 快速响应 |

## 🔧 技术实现

### 前端实现

1. **ai-settings.js**（全局共享）
   - 管理模型列表加载
   - 处理模型切换
   - localStorage持久化
   - 提供`getSelectedProviderId()`供其他模块调用

2. **导航栏badge**
   - 实时显示当前模型
   - 自动更新状态

3. **AI API调用统一处理**
   - 所有AI请求自动附加`provider_id`参数
   - 使用`getSelectedProviderId()`获取当前选择

### 后端实现

1. **AIManager增强**
   - `all_providers` 字典存储所有provider
   - `get_all_providers_status()` 返回模型列表
   - `use_provider(id)` 临时切换模型
   - `get_provider_id()` 获取当前ID

2. **API端点增强**
   - `/api/ai/models` - 获取模型列表
   - `/api/ai/select-model` - 选择模型
   - 所有AI端点支持 `provider_id` 参数

3. **自动恢复机制**
   - API调用后自动恢复原模型
   - 避免多用户环境下的冲突

## 💡 使用建议

### 场景1：个人学习（零成本）
```
主力：Gemini 2.0 Flash（完全免费）
备用：智谱AI GLM-4-Flash（国内访问）
```

### 场景2：公司项目（质量优先）
```
主力：Claude 3.5 Haiku（推理强）
备用：DeepSeek（性价比高）
```

### 场景3：日常 + 复杂混合
```
日常：Gemini（免费）
复杂：Claude（付费，按需切换）
```

### 场景4：速度优先
```
主力：Groq（免费，超快）
备用：Gemini（稳定）
```

## ❓ 常见问题

### Q1：选择模型后刷新页面会重置吗？
A：不会。选择会保存在浏览器localStorage中，永久有效（除非清除浏览器数据）。

### Q2：能否对不同功能使用不同模型？
A：当前版本是全局统一模型。如需此功能，可以通过API直接调用时传递不同的provider_id。

### Q3：切换模型需要重新登录吗？
A：不需要。切换是即时的，下一次AI调用就会使用新模型。

### Q4：配置了多个模型，哪个会被优先使用？
A：默认按优先级：Gemini → 智谱AI → DeepSeek → 通义千问 → Claude → OpenAI → Groq
但你可以通过设置面板手动选择任意已配置的模型。

### Q5：模型选择会影响其他用户吗？
A：不会。选择保存在用户浏览器本地，不同用户互不影响。

### Q6：如何查看当前使用的模型？
A：导航栏"AI设置"按钮上的badge显示当前模型简称。

## 🚨 故障排查

### 问题1：导航栏显示"未配置"
**原因**：没有配置任何AI模型的API密钥
**解决**：
1. 编辑.env添加至少一个模型的API密钥
2. 重启应用
3. 刷新页面

### 问题2：选择模型后提示"切换失败"
**原因**：该模型的API密钥未配置或无效
**解决**：
1. 检查.env中对应模型的API密钥
2. 验证密钥是否正确
3. 重启应用后重试

### 问题3：AI功能使用的不是我选择的模型
**原因**：API调用可能有缓存
**解决**：
1. 重新打开AI设置面板确认当前模型
2. 刷新页面
3. 清除浏览器缓存

### 问题4：设置面板打不开
**原因**：JavaScript加载失败
**解决**：
1. 检查浏览器控制台错误
2. 确认ai-settings.js文件已正确引入
3. 刷新页面

## 📚 相关文档

- `AI_CONFIG_GUIDE.md` - 完整的AI配置指南
- `QUICK_START_AI.md` - 快速开始
- `AI_MODEL_SELECTOR.md` - 模型选择器详细说明

---

## 🎉 快速开始

1. **配置一个免费模型**（推荐Gemini）
   ```bash
   # 在.env中添加
   GEMINI_API_KEY=your_key_here
   ```

2. **重启应用**
   ```bash
   python -m uvicorn backend.main:app --reload
   ```

3. **打开任意页面**

4. **点击"⚙️ AI设置"**

5. **看到Gemini已配置，开始使用！**

配置成功后，所有AI功能都可以使用了！🚀
